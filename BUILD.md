# Building from Source

Requires [Task](https://taskfile.dev) and Go 1.26+.

```sh
brew install go-task
task build
```

Run `source local-setup.zsh` which adds `$(pwd)/bin` to your `PATH` and sources the harness completions into your shell.
